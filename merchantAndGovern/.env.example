# Vite 环境变量（复制为 .env 后修改；须为 KEY=value 一行一项，勿使用 JS 对象写法）
VITE_API_BASE_URL=http://localhost:3000

# 高德地图：须为控制台「Key 名称」下勾选了「Web端(JS API)」的 Key，否则会出现 USERKEY_PLAT_NOMATCH
VITE_AMAP_KEY=

# 高德安全密钥 securityJsCode（控制台「安全密钥」页），加载地图前通过 _AMapSecurityConfig 注入，新版 Key 通常必填
VITE_AMAP_SECURITY_JS_CODE=
